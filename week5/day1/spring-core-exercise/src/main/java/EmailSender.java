public class EmailSender {
    public void send(String recipient, String message) {
        System.out.println("[Email] Sending message to: [" + recipient + "] - Contents: [" + message + "]");
    }
}
